import React from 'react';
import AppBar from '../AppBar/AppBar';

import './Header.scss';

function Header() {
  return (
    <div className="header">
      <AppBar />
    </div>
  );
}

export default Header;
